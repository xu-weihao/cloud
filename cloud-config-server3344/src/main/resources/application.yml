server:
  port: 3344

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:xu-weihao/cloud.git
          search-paths: config
        default-label: main

eureka:
  instance:
    instance-id: config-server
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/